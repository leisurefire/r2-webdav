{
	"name": "@r2-webdav/web",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "vite",
		"build": "tsc --noEmit && tsc -p functions/tsconfig.json && vite build",
		"typecheck": "tsc --noEmit && tsc -p functions/tsconfig.json",
		"deploy": "wrangler pages deploy dist --project-name webdav-ui"
	},
	"dependencies": {
		"@r2-webdav/shared-types": "0.1.0",
		"lucide": "^0.468.0",
		"lunar-typescript": "^1.8.6"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^5.20260717.1",
		"vite": "^6.0.0",
		"wrangler": "^4.112.0"
	}
}
